# Cloudflare Workers Assets ignore file
# 排除不需要上傳到 CDN 的檔案，降低部署 payload 大小

# 開發工具
.git/
.github/
.claude/
.vscode/
.idea/
node_modules/

# 後端腳本（後端不在 Worker 跑，由 GitHub Actions 跑後寫入 data/）
scripts/

# Worker 自己（由 wrangler 處理，不該當靜態資產）
worker.js
wrangler.jsonc

# 開發文件
README.md
.gitignore
.gitattributes

# 中間檔（不公開的原始資料）
data/_*.json
data/raw_*.json

# 開發／備份用的暫存頁（留在 repo 供參考，但不對外服務）
#   這些檔案沒有被任何頁面連結、也不在 sitemap 裡，卻可公開存取，
#   等於孤兒重複頁，只會佔用 Google 的爬取預算。
#   ⚠ index-mobile.html 不在此列：worker.js 手機版首頁會抓它，必須保留。
index-legacy-*.html
_preview_*.html
_ref_*.html

# 系統檔
.DS_Store
Thumbs.db
*.log
